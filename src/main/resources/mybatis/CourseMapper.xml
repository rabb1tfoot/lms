<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mymatix.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.course.mapper.CourseMapper">

    <sql id="selectListWhere">
        <if test="searchType != null  and searchValue != null">
            <choose>
                <when test="searchType == 'userId'">
                    and user_id Like concat('%' #{searchValue}, '%')
                </when>
                <when test="searchType == 'userName'">
                    and user_name Like concat('%' #{searchValue}, '%')
                </when>
                <when test="searchType == 'phoneNumber'">
                    and phone_number Like concat('%' #{searchValue}, '%')
                </when>
                <otherwise>
                    and (user_id Like concat('%' #{searchValue}, '%')
                    or user_name Like concat('%' #{searchValue}, '%')
                    or phone_number Like concat('%' #{searchValue}, '%')
                    )
                </otherwise>
            </choose>
        </if>
    </sql>

    <select id="selectListCount"
            parameterType="com.example.demo.course.model.CourseParam"
            resultType="long">
    select count(*)
    from course
    where 1 = 1
        <include refid="selectListWhere"></include>
    </select>


    <select id="selectList"
            parameterType="com.example.demo.course.model.CourseParam"
            resultType="com.example.demo.course.dto.CourseDto">
        select *
        from course
        where 1 = 1
            <include refid="selectListWhere"></include>
        order by reg_dt desc
        limit #{pageStart}, #{pageEnd}

    </select>
</mapper>